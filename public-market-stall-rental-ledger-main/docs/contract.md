# Contract

The contract stores commerce records with owner, target amount, released amount, status, and score.

Main method:

`settleCommerceRecord(id, actor, amount, score)`

Use this method after backend validation and wallet approval.
